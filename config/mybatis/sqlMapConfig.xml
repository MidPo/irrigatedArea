<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!--全局setting配置(缓存、延迟加载...)，根据需要添加 -->

	<!-- 配置别名 -->
	<typeAliases>
		<!-- 批量扫描别名
		指定包名，mybatis会自动扫描包中的po类，自动定义别名，别名就是类名 -->
		<package name="com.watersystem.po"/>
	</typeAliases>

	<!-- 配置mapper
	由于使用spring和mybatis的整合包进行mapper扫描，这里就不需要配置了。
	但是必须遵循mapper.xml和mapper.java文件同名且在一个目录下的规则
	 -->
<!-- 	 <mappers></mappers> -->

</configuration>